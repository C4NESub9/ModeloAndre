language: c++
sudo: required
dist: trusty

before_install:
  - sudo add-apt-repository ppa:beineri/opt-qt-5.14.1-trusty
  - sudo apt-get update -qq

install:
  - sudo apt-get -y install qt514[QTPACKAGE]

script:
  - source /opt/qt514/bin/qt511-env.sh
  - qmake Covid_19_console.pro
  - make
  - ./Covid_19_console
  